# NKUST_ML_AorticValve
# NKUST 機器學習期末：主動脈瓣 (Aortic Valve) 偵測專案

本專案旨在利用深度學習技術自動偵測 CT 斷層掃描影像中的主動脈瓣，並針對醫療影像數據稀缺問題實施多樣化增強策略。

## 🛠️ 安裝配置環境 (Installation)
* [cite_start]**執行環境**：Python 3.10 [cite: 19]
* [cite_start]**必要套件**：`ultralytics` (YOLOv12), `torch` (CUDA 12.1 加速), `opencv-python`, `numpy` [cite: 19, 20]
* [cite_start]**硬體需求**：建議使用具備 NVIDIA GPU 之環境執行以加速訓練過程 [cite: 19]。

## 📂 重要模組與資料處理 (Modules & Data)
* [cite_start]**資料前處理**：程式自動解壓 `training_image` 與 `training_label` [cite: 15, 16]。
* [cite_start]**資料配對**：透過 `move_patients` 函式將影像與 YOLO 格式標籤進行精準配對 [cite: 16, 18]。
* **離線增強 (Offline Augmentation)**：包含水平翻轉、垂直翻轉與隨機縮放 (0.8~1.2)，有效擴充訓練樣本多樣性。
* **在線增強 (Online Augmentation)**：訓練期間動態調整 HSV 色域與影像縮放比例。

## 🚀 訓練與預測流程
1. [cite_start]**模型架構**：採用 YOLO12n 輕量化模型 [cite: 18]。
2. [cite_start]**訓練設定**：設定 `epochs=50`、`imgsz=640`、`batch=16` [cite: 30]。
3. [cite_start]**預測輸出**：讀取訓練產出之 `best.pt` 權重，針對測試集產出符合規範之 `images.txt` 座標檔 [cite: 27]。

## 📊 實驗成效
* **Validation Score**: 0.982
* **Public Score**: 0.952
